package ca.qc.grasset._420_pa4_ag.tp01;

import static org.junit.Assert.*;

import org.junit.Assert;
import org.junit.Test;

public class UtilitaireTableauTest_Test2 {

	@Test
	public void test() {
	
        final int[] x = { 0, 1, 0 };

        final int y = 2;
        
        int indicateur = new UtilitaireTableau().chercherDernierePositionZero(x);

        if (indicateur == 2) {
            System.out.println("La dernier  position de " + x + " est " + indicateur);
        } else {
        	System.out.println("La position de " + y + " n'est pas trouvée. Retourne " + indicateur);
        }

        Assert.assertTrue(indicateur == 2);
    }

}
	
	